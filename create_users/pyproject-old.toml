[tool.poetry]
name = "create_users"
version = "0.1.0"
description = "create-users using crewAI"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = ">=3.10,<=3.13"
crewai = {extras = ["tools"], version = "^0.28.8"}

[tool.poetry.scripts]
create_users = "create_users.main:run"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"